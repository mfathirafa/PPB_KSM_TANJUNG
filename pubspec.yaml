name: ksmobile
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.2
  qr_flutter: ^4.1.0
  cupertino_icons: ^1.0.8  # pindahkan ke sini (di bawah dependencies)

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/QR.jpg
    
